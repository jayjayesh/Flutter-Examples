import 'package:flutter/material.dart';

class Screen1Class extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen1')),
      body: Container(),
    );
  }
}
